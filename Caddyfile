cms.patarutera.pe, www.cms.patarutera.pe {
    # Caddy detectará estos nombres de host y automáticamente
    # obtendrá los certificados SSL/TLS de Let's Encrypt.
    
    # 1. Configuración de archivos estáticos
    # Esto apunta a la carpeta /srv dentro del contenedor.
    # Usaremos esto como la ubicación de tu CMS.
    root * /srv
    file_server

    # 2. Manejo de errores (opcional pero recomendado)
    # Por ejemplo, para mostrar una página 404 personalizada
    # handle_errors {
    #     respond "{http.error.status_code} {http.error.status_text}"
    # }

    # Puedes añadir más directivas aquí según tu CMS (ej. PHP, headers, etc.)
}
